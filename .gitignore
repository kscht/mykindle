# Контент пользователя — не трекаем (но папки сохраняем через .gitkeep)
inbox/*
staging/*
.state/*
!inbox/.gitkeep
!staging/.gitkeep
!.state/.gitkeep

# Приватный локальный контекст
CLAUDE.local.md
*.local.md
*.local

# Локальные настройки Claude Code
.claude/settings.local.json

# OS / редакторы
.DS_Store
*.swp
*~
